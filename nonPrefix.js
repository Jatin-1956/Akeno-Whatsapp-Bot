module.exports = { msg }
const fun = require('./lib/fun.js')
async function msg(message, color, p, time) {
	if (message.type == 'chat') {
		const text = message.body.toLowerCase() 
		switch (true) {
			case text.startsWith('an error'): 
				 sclient.reply(message.from, 'Link!', message.id)
				 break
			case text.startsWith("omae wa mou shindeiru"):
				return sclient.reply(message.from, 'NANI!!', message.id)
				break
			case text.startsWith('i love you slave'):
			case text.startsWith('i love youu slave'):
			case text.startsWith('i love u slave'):
			case text.startsWith('i love you too slave'):		
			case text.startsWith('i love u too slave'):
			case text.startsWith('ily slave'):
			case text.startsWith('i love you akeno'):
			case text.startsWith('i love youu akeno'):
			case text.startsWith('i love u akeno'):
			case text.startsWith('i love you too akeno'):		
			case text.startsWith('i love u too akeno'):
			case text.startsWith('ily akeno'):
			case text.startsWith('i love you bot'):
			case text.startsWith('i love youu bot'):
			case text.startsWith('i love u bot'):
			case text.startsWith('i love you too bot'):		
			case text.startsWith('i love u too bot'):
			case text.startsWith('ily bot'):
					return sclient.reply(message.from, 'I love you toooo, Senpai 😍 and I will always keep loving you 😘', message.id)
				break
			case text.startsWith("what's your name slave?"): 
			case text.startsWith("what's your name slave"):
			case text.startsWith('whats your name slave?'):
			case text.startsWith('whats your name slave'):
			case text.startsWith('what is your name slave?'):
			case text.startsWith('what is your name slave'):	
			case text.startsWith("what's ur name slave?"): 
			case text.startsWith("what's ur name slave"):
			case text.startsWith('whats ur name slave?'):
			case text.startsWith('whats ur name slave'):
			case text.startsWith('what is ur name slave?'):
			case text.startsWith('what is ur name slave'):				
			case text.startsWith('who are you slave'):
			case text.startsWith('who are u slave'):
			case text.startsWith('hey slave'):
			case text.startsWith('hi slave'):
			case text.startsWith('hello slave'):
			case text.startsWith("what's your name akeno?"): 
			case text.startsWith("what's your name akeno"):
			case text.startsWith('whats your name akeno?'):
			case text.startsWith('whats your name akeno'):
			case text.startsWith('what is your name akeno?'):
			case text.startsWith('what is your name akeno'):	
			case text.startsWith("what's ur name akeno?"): 
			case text.startsWith("what's ur name akeno"):
			case text.startsWith('whats ur name akeno?'):
			case text.startsWith('whats ur name akeno'):
			case text.startsWith('what is ur name akeno?'):
			case text.startsWith('what is ur name akeno'):				
			case text.startsWith('who are you akeno'):
			case text.startsWith('who are u akeno'):
			case text.startsWith('hey akeno'):
			case text.startsWith('hi akeno'):
			case text.startsWith('hello akeno'):
			case text.startsWith("what's your name bot?"): 
			case text.startsWith("what's your name bot"):
			case text.startsWith('whats your name bot?'):
			case text.startsWith('whats your name bot'):
			case text.startsWith('what is your name bot?'):
			case text.startsWith('what is your name bot'):	
			case text.startsWith("what's ur name bot?"): 
			case text.startsWith("what's ur name bot"):
			case text.startsWith('whats ur name bot?'):
			case text.startsWith('whats ur name bot'):
			case text.startsWith('what is ur name bot?'):
			case text.startsWith('what is ur name bot'):				
			case text.startsWith('who are you bot'):
			case text.startsWith('who are u bot'):
			case text.startsWith('hey bot'):
			case text.startsWith('hi bot'):
			case text.startsWith('hello bot'):
					return sclient.reply(message.from, "Kon'nichiwa, I'm Akeno! (close friend of Jatin (Owner-sama)🥰) and it's so nice to meet and be with you all😍 Please use me as freely as you want 🥰", message.id)
				break				
			case text.startsWith('fuck you slave'):
			case text.startsWith('fuck u slave'):
			case text.startsWith('fk you slave'):
			case text.startsWith('fk u slave'):
			case text.startsWith('fuck you bot'):
			case text.startsWith('fuck u bot'):
			case text.startsWith('fk you bot'):
			case text.startsWith('fk u bot'):
			case text.startsWith('fuck you akeno'):
			case text.startsWith('fuck u akeno'):
			case text.startsWith('fk you akeno'):
			case text.startsWith('fk u akeno'):
					return sclient.reply(message.from, 'You can fuck me anytime, anywhere Senpai 😉🥵', message.id)
				break
			case text.startsWith('fuck off slave'):
			case text.startsWith('fk off slave'):
			case text.startsWith('fuck off akeno'):
			case text.startsWith('i hate you akeno'):
			case text.startsWith('i hate u akeno'):
			case text.startsWith('fk off akeno'):
			case text.startsWith('fuck off bot'):
			case text.startsWith('fk off bot'):
				return sclient.reply(message.from, "I'm sorry if I was unsatisfactory. Please let me make it upto you 😓😉😏", message.id)
				break
			case text.startsWith('arigato slave'):
			case text.startsWith('thank you slave'):
			case text.startsWith('thank u slave'):
			case text.startsWith('tq slave'):
			case text.startsWith('arigato akeno'):
			case text.startsWith('thank you akeno'):
			case text.startsWith('thank u akeno'):
			case text.startsWith('tq akeno'):
			case text.startsWith('arigato bot'):
			case text.startsWith('thank you bot'):
			case text.startsWith('thank u bot'):
			case text.startsWith('thanks bot'):
			case text.startsWith('thanks slave'):
			case text.startsWith('thanks akeno'):
			case text.startsWith('tq bot'):
					return sclient.reply(message.from, 'Thank you Senpai... Always my pleasure🥰', message.id)
				break
			case text.startsWith('we missed you akeno'):
			case text.startsWith('we missed u akeno'):
			case text.startsWith('i missed you akeno'):
			case text.startsWith('i missed u akeno'):
			case text.startsWith('we missed you slave'):
			case text.startsWith('we missed u slave'):
			case text.startsWith('i missed you slave'):
			case text.startsWith('i missed u slave'):
			case text.startsWith('we missed you bot'):
			case text.startsWith('we missed u bot'):
			case text.startsWith('i missed you bot'):
			case text.startsWith('i missed u bot'):
			case text.startsWith('we missed you too akeno'):
			case text.startsWith('we missed u too akeno'):
			case text.startsWith('i missed you too akeno'):
			case text.startsWith('i missed u too akeno'):
			case text.startsWith('we missed you too slave'):
			case text.startsWith('we missed u too slave'):
			case text.startsWith('i missed you too slave'):
			case text.startsWith('i missed u too slave'):
			case text.startsWith('we missed you too bot'):
			case text.startsWith('we missed u too bot'):
			case text.startsWith('i missed you too bot'):
			case text.startsWith('i missed u too bot'):
				return sclient.reply(message.from, '🤗 I missed you too 😍😘💖 and I hope I never leave again until Owner-sama does anything stupid although I will still be asleep when Jatin (Owner-sama) is asleep', message.id) }
				break
			case text.startsWith("cmon akeno"):
			case text.startsWith("c'mon akeno"):
			case text.startsWith("aww c'mon akeno"):
			case text.startsWith("aww cmon akeno"):
			case text.startsWith("cmon slave"):
			case text.startsWith("c'mon slave"):
			case text.startsWith("aww c'mon slave"):
			case text.startsWith("aww cmon slave"):
			case text.startsWith("cmon bot"):
			case text.startsWith("c'mon bot"):
			case text.startsWith("aww c'mon bot"):
			case text.startsWith("aww cmon bot"):
				return sclient.reply(message.from, 'Oops.. Sorry 😄😜', message.id)
				break
			case text.startsWith("whats up akeno"):
			case text.startsWith("what's up akeno"):
			case text.startsWith("whats up slave"):
			case text.startsWith("what's up slave"):
			case text.startsWith("whats up bot"):
			case text.startsWith("what's up bot"):
					fun.flirty()
                    .then(async (body) => {
                        const tod = body.split('\n')
                        const randomTod = tod[Math.floor(Math.random() * tod.length)]
                        return sclient.reply(message.from, randomTod, message.id)
                    })
                    .catch(async (err) => {
                        console.error(err)
                        return sclient.reply(message.from, 'Sorry babe, something went wrong!!', message.id)
                    })
				break
			case text.startsWith('y akeno'):
					return sclient.sendMp4AsSticker(message.from, `https://media.giphy.com/media/6aPHIMzTnOsTMiztAp/giphy.mp4`);
			case text.startsWith('pick me up slave'):
			case text.startsWith('pick me up akeno'):
			case text.startsWith('pick me up bot'):
					fun.pickup()
                    .then(async (body) => {
                        const tod = body.split('\n')
                        const randomTod = tod[Math.floor(Math.random() * tod.length)]
                        return sclient.reply(message.from, randomTod, message.id)
                    })
                    .catch(async (err) => {
                        console.error(err)
                        return sclient.reply(message.from, 'Sorry babe, something went wrong!!', message.id)
                    })
		       /* eg case text.startsWith('your text'): 
				sclient.reply(message.from, 'response' message.id) 
				break*/
		}
	}
	if (p) return console.log('[RECV]', color(time, 'yellow'), 'Message from', color(message.sender.pushname))
	if (!p) return console.log('[RECV]', color(time, 'yellow'), 'Message from', color(message.sender.pushname), 'in', color(message.chat['name']))
}

			
